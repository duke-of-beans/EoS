GITHUB SETUP — Eye of Sauron
=============================
Generated by Sprint 2 EOS Lift — 2026-03-21

The local git repo is initialized and has 3 commits on branch: main
Current remote is a placeholder (YOUR_GITHUB_USER/YOUR_REPO_NAME) and must be replaced.

OPTION A — gh CLI (recommended, run in cmd from D:\Projects\eye-of-sauron):
-----------------------------------------------------------------------------
  cd D:\Projects\eye-of-sauron
  gh repo create duke-of-beans/eye-of-sauron --public --source=. --remote=origin --push

This will:
  1. Create the GitHub repo at github.com/duke-of-beans/eye-of-sauron
  2. Set origin remote to the new repo
  3. Push all commits including this sprint's lift commit

OPTION B — Manual (if gh CLI unavailable):
-------------------------------------------
  1. Go to https://github.com/new
  2. Name: eye-of-sauron
  3. Owner: duke-of-beans
  4. Public repo, no README/gitignore (repo already has these)
  5. Then run in cmd:

  cd D:\Projects\eye-of-sauron
  D:\Program Files\Git\cmd\git.exe remote set-url origin https://github.com/duke-of-beans/eye-of-sauron.git
  D:\Program Files\Git\cmd\git.exe push -u origin main

NOTE: The .gitignore already excludes .pre-nuclear files so they will NOT be pushed.
The current remote placeholder will cause push failures until one of the above steps is done.

COMMITS THAT WILL BE PUSHED:
  bfdea8a - Emergency: Remove homoglyph attacks
  3a42f3b - Initial commit: Eye of Sauron core
  3924079 - lift: CLAUDE_INSTRUCTIONS, STATUS.md, .gitignore, pre-nuclear manifest
