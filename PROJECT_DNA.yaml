project_id: eye-of-sauron
name: Eye of Sauron (EOS)
tagline: "Deep code analysis and pattern detection across entire codebases"
status: pre-system-lift

# Functional tool with confirmed scan output artifacts. CLI operational.
# Pre-system state: no CLAUDE_INSTRUCTIONS, no STATUS.md, many .pre-nuclear backup files.
# Highest strategic priority of the three lift targets — GregLite Sprint 44.0 depends on it.

wave: 5a
portfolio_tier: enterprise_infrastructure
enterprise_map_holdco: infrastructure_holdco_tbd
# May move to tech_holdco — depends on whether EOS is positioned as GregLite internal
# module or standalone enterprise product. Architecture decision pending.

proof_tenant: GregLite
# GregLite Sprint 44.0 (Code Graph Index) is the primary integration target.

tech_stack:
  - Node.js (>=16.0.0 per package.json engines field)
  - chalk 5.x (CLI output formatting)
  - commander 14.x (CLI argument parsing)
  - ora 8.x (terminal spinners)
  - pkg 5.8.1 (binary compilation — builds .exe / macOS / Linux executables)
  - webpack / @vercel/ncc (bundling)
  # NO framework. Pure Node.js CLI tool.
  # Has a web UI component: eye-of-sauron-dashboard.html, eye-of-sauron-ui.html
  # Has a REST API server: server.js
  # Has an Express server: serve-ui.mjs

primary_language: JavaScript
# Pre-TypeScript. Port to TypeScript recommended to align with GregLite stack.

entry_point:
  cli: "sauron-cli.js (node sauron-cli.js [options])"
  packaged: "standalone-launcher.cjs (bin: sauron-scan, sauron-archive)"
  api: "server.js (REST API mode)"
  ui_server: "serve-ui.mjs"
  core_module: "SauronCore.js"
  index: "index.js"

repo: null
# GAP: Not on GitHub. Create: duke-of-beans/eye-of-sauron

vercel_project: null

last_known_working_state: >
  Tool is functional. Confirmed by presence of scan output artifacts in root:
  full-scan.json, scan-results.json, full-scan-all-analyzers-2025-06-22T*.json,
  eos-self-scan.json, system-health.json, operational-health-report.json.
  The tool has scanned itself (eos-self-scan.json) and generated health reports.
  scan profiles confirmed working: quick, standard, deep (per .sauronrc.json).
  analyzers/, core/, reporters/, cli/, utils/, src/ directory structure in place.
  Many .pre-nuclear backup files exist — these are pre-"nuclear cleaner" snapshots,
  preserved intentionally. Do NOT delete without David's explicit approval.

strategic_role: >
  GregLite code intelligence layer. Powers Code Graph Index (Sprint 44.0).
  GregLite Sprint 44.0 is blocked until EOS integration or replacement is defined.
  Also positioned as standalone enterprise code analysis tool — two-channel value:
  internal GregLite module AND commercial product for enterprise codebases.
  D:\Projects\_Archive\T-App-tribunal-assistant\ contains eye-of-sauron.cjs and
  sauron-report files — review before any modernization to understand prior integration.

open_questions:
  - "EOS as GregLite internal sidecar module vs standalone enterprise service — still both?"
  - "Port to TypeScript? Required to integrate cleanly into GregLite TypeScript codebase."
  - "Relationship to TESSRYX (dependency intelligence) — overlap or complement?"
  - "Rename for enterprise positioning? 'Eye of Sauron' may not be the right brand."
  - ".pre-nuclear backup files — David approval needed before any cleanup"
  - "tribunal-assistant in _Archive has eye-of-sauron.cjs — review before EOS modernization"
  - "server.js and serve-ui.mjs — is the web UI / REST API mode still in scope?"

dependencies_on:
  - Node.js file system (local execution — no external APIs required)
  - Target codebase access (reads files from any directory passed as --input)

depended_on_by:
  - GregLite (Sprint 44.0 Code Graph Index — primary integration target, currently blocked)
  - TESSRYX (potential — dependency mapping may overlap with EOS pattern detection)

notes: >
  Highest strategic priority of the three lift targets. GregLite Sprint 44.0 (Code Graph
  Index) is blocked until EOS is properly integrated or replaced by the Code Graph Index spec.
  Many .pre-nuclear backup files in root — do not delete, flag for David review only.
  D:\Projects\_Archive\T-App-tribunal-assistant\ contains eye-of-sauron.cjs worth reviewing.
  Read D:\Projects\GregLite\STATUS.md and Sprint 44.0 spec before any EOS session.
  dist/ directory exists — compiled binaries may already be present.
